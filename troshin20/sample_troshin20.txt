#include <bits/stdc++.h>
using namespace std;
#define hotel trivago
#define pii pair<int, int>
#define fi first
#define se second
#define int long long
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()

template<class T>
istream&operator>>(istream&in,vector<T>&a)
{for(auto&i:a)in>>i;return in;}template
<class T>ostream&operator<<(ostream&out,
vector<T>&a){for(auto&i:a)out<<i<<' ';return out;}


void solve() {
   
}

signed main(void) {
    ios_base::sync_with_stdio(0);
    cout.tie(0); cin.tie(0);
#ifdef troshin20
    freopen(".in", "r", stdin);
    freopen(".out", "w", stdout);
#endif
    int t; cin >> t; while(t--)
    solve();
    return -0;
}
